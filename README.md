# G3

Pour lancer notre jeu BR-Type il faut :
	- Dans un terminal, se placer dans le même dossier que celui de ce README.
	- Se donner les permissions d'exécution de notre .sh avec la commande "chmod +x BRType.sh".
	- Taper la commande ./BRType.sh.

